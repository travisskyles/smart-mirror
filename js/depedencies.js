'use strict';

/**
 * third party dependencies. Any third party modules are installed via npm and added here. Then link within the _getScripts in modules file
 */
const dependencies = {
  'ejs': 'node_modules/ejs/ejs.min.js',
  'moment': 'node_modules/moment/min/moment.min.js',
}